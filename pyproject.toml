[project]
name = "problemset-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.136.0",
  "uvicorn>=0.49.0",
  "PyJWT>=2.13.0",
  "bcrypt>=5.0.0",
  "python-multipart>=0.0.32",
  "libsql>=0.1.11",
]

[tool.vercel]
entrypoint = "api.index:app"
